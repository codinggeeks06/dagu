import { editor } from 'monaco-editor';
import { Editor } from './Editor';

export interface IEditorProps extends editor.IEditorConstructionOptions {
  theme?: string;
  language?: string;
  value?: string;
  readOnly?: boolean;
  onChange?: (value: string, editor: editor.IStandaloneCodeEditor,
    instance: Editor) => void;
  onReady?: (editor: editor.IStandaloneCodeEditor, instance: Editor) => void;
}