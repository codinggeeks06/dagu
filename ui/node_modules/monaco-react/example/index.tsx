import * as React from 'react';
import * as ReactDOM from 'react-dom';
import Editor from '../src';

function App() {
  return <Editor value="//test" />
}

ReactDOM.render(<App />, (window as any).root);